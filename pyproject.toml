[project]
name = "mapwa"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pygame-ce",
    "pygbag>=0.9.3",
    "pytest",
    "watchdog",
]

[dependency-groups]
dev = [
    "pytest-xdist>=3.8.0",
    "ruff>=0.15.14",
]
